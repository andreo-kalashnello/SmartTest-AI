# Публічний URL бекенду (порожній = відносні запити до того ж origin)
NEXT_PUBLIC_API_URL=

# Тимчасово: dashboard без логіну (за замовчуванням увімкнено). Після беку: false
NEXT_PUBLIC_SKIP_AUTH=true
